mutation ChangePasswordFromToken($token: String!, $newPassword: String!) {
  changePasswordFromToken(
    options: { token: $token, newPassword: $newPassword }
  ) {
    ...RequiredUserResponse
  }
}
